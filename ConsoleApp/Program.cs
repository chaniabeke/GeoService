using GeoService.Domain.Managers;
using GeoService.EF.DataAccess;
using System;

namespace ConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
           // ContinentManager manager = new ContinentManager(new UnitOfWork(new DataContext()));

           // manager.AddContinent(new GeoService.Domain.Models.Continent(1,"Asia"));
        }
    }
}
